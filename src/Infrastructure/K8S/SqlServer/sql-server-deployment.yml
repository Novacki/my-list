apiVersion: apps/v1
kind: Deployment
metadata:
  name: sql-server-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sql-server
  template:
    metadata:
      labels:
        app: sql-server
    spec:
      containers:
      - name: sql-server
        image: mcr.microsoft.com/mssql/server:2017-latest
        env:
          - name: ACCEPT_EULA
            valueFrom:
              secretKeyRef:
                name: sql-server-secret 
                key: ACCEPT_EULA

          - name: SA_PASSWORD
            valueFrom:
              secretKeyRef:
                name: sql-server-secret 
                key: SA_PASSWORD  
        resources:
          requests:
            cpu: 130m
            memory: 500Mi
          limits:
            cpu: 280m
            memory: 800Mi
        ports:
        - containerPort: 1433
          name: sql-server